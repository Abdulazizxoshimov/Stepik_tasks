package main

import "fmt"

func fibonacci(n int) int{
    a, b := 1, 1
    for i := 1; i < n; i++ {
        a, b = b, a + b
    }
    return a
}
func main(){
    var a int
    fmt.Scan(&a)
    fmt.Print(a)
}