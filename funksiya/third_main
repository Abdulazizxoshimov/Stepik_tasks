package main
import "fmt"

func vote(x int, y int, z int) int {
    //print your code here
    if x + y + z <= 1 {
        return 0
    } else {
        return 1
    }
    
}
func main(){
    var a, b, c int
    fmt.Scan(&a, &b, &c)
    fmt.Print(vote(a,b, c))
}