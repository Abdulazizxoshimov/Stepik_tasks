package main

import "fmt"

func main(){
    var a, c int
    fmt.Scan(&a)
    for a <= 100 {
        if a > 9{
            fmt.Println(a)
            c = -1
        }
        fmt.Scan(&a)
    }
    if c != -1 {
        fmt.Println(0)
    }
}
